#apiVersion: v1
#kind: Service
#metadata:
#  name: kafka-external-service
#  labels:
#    app: kafka
#spec:
#  selector:
#    app: kafka
#  type: LoadBalancer
#  ports:
#    - port: 9992  # External port (different from container port)
#      targetPort: 9092
#      protocol: TCP